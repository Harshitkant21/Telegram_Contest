In both channelProfileActivity and UserProfileActivity
getCurrentThemeResId this is not found in the theme file instead it has getCurrentTheme 

private TLRPC.chat currentChannel; instead of this i add private TLRPC.TL_channel currentChannel; check once is it correct or not once also these are not found means TLRPC is not found by it
because of it also it doesn't find any about part 

setImage is not found in all files channelProfileActivity UserProfileActivity GroupProfileActivity
ImageLoader.getInstance().setImage(channelAvatar,"https://cdn.telegram.org/file/"+ photoUrl, null,null);

gift_tab_view is missing in layout

also that about error is present in both files channelProfileActivity and GroupProfileActivity



setImage error have missing avatarUrl this is in GroupProfileActivity


both are in UserProfileActivity
giftTbContainer is missing 
GiftSendActivity is missing


giftsContainer is also missing in GiftsTabView